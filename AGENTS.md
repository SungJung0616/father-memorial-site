# 추모 사이트 작업 원칙

- `docs/`의 최신 결정과 가족 확인 정보를 프로젝트 기준으로 사용한다.
- 작은 수정은 대상 파일, 직접 연결된 컴포넌트·API·스타일만 먼저 확인한다.
- `node_modules`, `.next`, `out`, `.pnpm-store`는 검색하거나 검토하지 않는다.
- 사용자가 검토를 요청한 경우에는 먼저 원인과 변경 방향을 설명하고, 승인된 범위만 구현한다.
- 공개 화면 변경은 큰 글자, 큰 터치 영역, 명확한 한국어와 40대 후반–70대 이용자의 모바일 사용성을 우선한다.
- 개인정보와 미승인 사진은 공개하지 않으며 방문자 제출은 관리자 승인 후 게시한다.
- 작은 UI 변경은 관련 lint로 확인하고, 전체 빌드는 배포 직전에 한 번 실행한다.
- 여러 수정은 가능한 한 한 배포로 묶고, 배포 후 변경된 화면과 핵심 API만 확인한다.
- 실제 비밀번호, 액세스 키와 개인 연락처를 코드·문서·Git 기록에 넣지 않는다.

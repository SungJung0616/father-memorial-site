# 배포·저장·운영 설계

## 권장 AWS 구성

- Netlify Free: 공개 사이트와 관리자 웹 화면의 우선 배포 대상
- Amazon S3 private bucket: 원본 사진·영상과 방문자 제출 자료
- Amazon S3 published bucket 또는 보호된 prefix: 승인된 웹용 이미지·영상
- Amazon CloudFront: 한국과 미국 방문자에게 빠르게 사진 전달
- Amazon Cognito: 관리자 로그인과 역할 권한
- AWS Lambda: 썸네일, WebP/JPEG 사본, 영상 포스터 이미지 생성
- DynamoDB: 사진 메타데이터, 분류, 번역, 승인 상태
- AWS Budgets: 월 비용 경고

## 현재 배포 결정

- 사용자가 기존 `sj` 포트폴리오를 Netlify 무료 플랜에서 운영하고 있으므로 익숙한 배포 흐름을 우선 활용한다.
- 추모 사이트는 기존 포트폴리오에 섞지 않고 Netlify의 별도 사이트로 만든다.
- 별도 저장소, 별도 빌드 설정, 별도 환경변수, 별도 도메인을 사용한다.
- Netlify는 화면 배포를 담당하고 AWS는 사진·영상, 인증, 메타데이터를 담당한다.
- 실제 셋업 직전에는 당시 Netlify 무료 플랜의 대역폭·빌드·함수 제한을 다시 확인한다.

## 실제 AWS 설정 현황 (2026-09-07)

- 리전: 아시아 태평양(서울), `ap-northeast-2`
- 비공개 미디어 버킷: `father-memorial-media-905418126737`
- 객체 소유권: ACL 비활성화, 버킷 소유자 적용
- 퍼블릭 액세스: 네 가지 항목 모두 차단
- 기본 암호화: Amazon S3 관리형 키(SSE-S3)
- 버전 관리: 활성화
- 비용 태그: `Project=father-memorial`
- 수명 주기 규칙: 이전 객체 버전은 30일 후 영구 삭제하고, 완료되지 않은 멀티파트 업로드는 7일 후 제거
- CORS: `https://father-memorial-test.netlify.app`와 로컬 개발 주소에서의 `PUT`만 허용
- IAM 정책: `FatherMemorialPendingUploadOnly` (`pending/*`에 `s3:PutObject`만 허용)
- IAM 사용자: `father-memorial-netlify-uploader` (콘솔 로그인 권한 없음)
- Netlify 비공개 환경변수: AWS 키, 버킷, 리전, 업로드 활성화 값 등록 완료. 실제 값은 문서와 코드에 기록하지 않는다.
- Netlify 업로드·제출 완료·공개 조회 함수 배포 완료.
- DynamoDB 테이블: `father-memorial-content` (온디맨드 과금, 시점 복구 활성화)
- Cognito 관리자 사용자 풀: `ap-northeast-2_ftJFI6Pfw`
- Cognito 앱 클라이언트: `5ej6m31ke38bv61c1iug2cr7ud`
- 초기 관리자 이메일: `sung.gpslgx@gmail.com` (임시 비밀번호는 문서와 코드에 기록하지 않는다.)
- 관리자 화면 `/admin`은 Cognito 로그인 후 제출 목록 조회, 승인·보류·비공개 처리를 제공한다.
- 초대용 임시 비밀번호는 7일 동안 유효하다. 만료되거나 잊은 경우 `/admin`의 이메일 확인 코드 방식으로 새 비밀번호를 설정한다.
- 방문자 제출은 S3 `pending/`에 원본을 올린 뒤 DynamoDB에 `PENDING` 또는 `FAMILY` 상태로 기록된다.
- 승인된 게시물은 S3 `published/`로 복사되고 `/api/memories`를 통해 공개 화면에 나타난다.
- CloudFront와 자동 이미지 최적화는 아직 연결하지 않았다.
- Netlify의 `Powered by Netlify` 공개 배지는 2026-09-07 비활성화했다. 모바일 하단 메뉴와 겹치지 않으며 재배포 없이 적용된다.

### 현재 보관 자료

- 은퇴식 가족사진 제출 1건을 `PENDING` 상태로 검토함에 연결했다.
- 대표사진 후보 1번 원본을 `s3://father-memorial-media-905418126737/pending/hero-candidates/KakaoTalk_20260907_100458847.jpg`에 비공개 보관했다.

AWS Budgets에는 모든 AWS 서비스를 대상으로 월 `US$20` 예산을 만들었다. 실제 비용이 `US$10`과 `US$20`을 초과할 때 AWS 계정 이메일로 알림을 보낸다. 알림만 제공하며 리소스를 자동 중지하지 않는다.

공개 브라우저가 S3 원본 주소를 직접 사용하지 않게 한다. 업로드도 애플리케이션 서버를 통과시키지 않고, 짧게 유효한 presigned URL을 받아 S3로 직접 전송한다.

## 저장 정책

- 원본: 비공개, 수정 금지
- 웹용 사진: 긴 변 2,000px 이하 WebP/JPEG
- 썸네일: 갤러리용 소형 이미지
- 영상: 원본과 웹 재생용 버전을 구분
- 삭제는 즉시 영구 삭제하지 않고 유예 기간을 둔다.
- 오래된 원본의 별도 보존 사본은 S3 Glacier Deep Archive로 이동할 수 있다.

## 월 비용 가정

2026-08-28 기준 단순 추정이다. S3 Standard 첫 구간을 GB당 월 $0.023, Glacier Deep Archive를 GB당 월 $0.00099로 계산한다. CloudFront의 월 1TB 전송과 1,000만 HTTP/HTTPS 요청 무료 범위 안에 있다고 가정한다.

| 원본 용량 | 웹 사본 포함 추정 저장량 | S3 Standard 저장비 | 예상 전체 월 비용 |
|---:|---:|---:|---:|
| 100 GB | 125 GB | 약 $2.88 | 약 $3–7 |
| 500 GB | 625 GB | 약 $14.38 | 약 $15–25 |
| 1 TB | 1.25 TB | 약 $29.44 | 약 $30–45 |
| 2 TB | 2.5 TB | 약 $58.88 | 약 $60–85 |

전체 비용에는 소규모 요청·변환·데이터베이스 비용의 여유분을 포함했다. CloudFront 전송량이 월 1TB를 넘거나 원본 영상을 자주 재생하면 추가 비용이 커질 수 있다.

## 비용 절감 원칙

- 갤러리에서는 원본이 아닌 썸네일과 웹용 사본만 제공한다.
- 동영상은 자동재생하지 않고 사용자가 눌렀을 때만 재생한다.
- 원본 다운로드는 관리자에게만 허용한다.
- CloudFront 캐시를 길게 유지한다.
- 오래된 원본의 보존 사본은 Glacier Deep Archive로 이동한다.
- AWS Budgets의 실제 비용 $10, $20 알림을 유지한다.

## 백업 주의사항

S3의 내구성이 높아도 실수로 삭제하거나 계정이 침해될 가능성은 별개다. 버전 관리 또는 별도 아카이브 사본을 두고, 가족이 보관하는 외장 저장장치에도 원본 사본 하나를 유지한다.

## 다음 연결 순서

1. [완료] Netlify 함수용 최소 권한 IAM 정책을 연결한다.
2. [완료] 제출 기록용 DynamoDB와 관리자 인증용 Cognito를 만든다.
3. [완료] 짧게 유효한 S3 presigned URL과 제출 완료 API를 구현한다.
4. [완료] 일반 사용자는 한 번에 최대 10개를 업로드하도록 제한한다.
5. [완료] 업로드 파일 종류·크기를 검증하고 `pending/`에 저장한다.
6. [구현 완료·운영 확인 필요] 관리자 승인 시 `published/`로 복사하고 공개 목록에 연결한다.
7. [다음 단계] 대표사진 최대 5장 관리, 썸네일·웹용 사본 자동 생성, CloudFront 배포를 구현한다.

## 보안 보류 항목

- AWS 루트 계정 비밀번호 변경
- AWS 루트 계정 MFA 활성화
- 일상적인 운영에서는 루트 계정 대신 제한된 관리자 계정 사용

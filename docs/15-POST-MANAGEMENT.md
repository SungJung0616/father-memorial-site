# 게시물 운영 — 2026-09-08

- 기존 Cognito 역할 유지: admin 전체 관리, family 편집·공개·가족 보관 전환, reviewer 읽기. 휴지통 조회·이동·복구는 admin 전용이다.
- 제목, 본문, 작성자(비공개), 관계, 현재 공개 요청, 분류, 관리자 메모를 저장한다. 현재 공개 요청과 실제 게시 상태는 별도로 표시한다.
- `consent`는 수정하지 않는다. 최초 관리 변경 때 `originalSubmission`에 당시의 제출자·공개 요청·동의를 `if_not_exists`로 보존한다. 과거에 이미 수정된 기록을 추정 복원하지 않는다.
- 원본 공개 요청/동의가 부족한 재공개는 추가 동의 확인 메모와 명시적 확인을 요구한다.
- `FAMILY`와 `TRASH`는 공개 목록·상세·사진첩·관리 대표사진에서 제외한다. 공개 API는 no-store 및 기본 테이블의 강한 일관성 조회로 GSI의 이전 상태를 재검증한다. 브라우저 목록 캐시를 재사용하지 않는다.
- 이미 열린 화면은 새로고침/재진입 시 갱신된다. 이미 발급된 signed 사진 URL의 최대 1시간 유예는 가족이 허용했다. 다운로드된 사본은 회수할 수 없다.
- 휴지통에서 복구하면 FAMILY로 돌아오며 자동 재공개하지 않는다. S3 파일, publishedFiles, 대표사진 설정, 좋아요 등은 그대로 보존한다.
- publishedFiles가 있는 게시물의 재공개는 기존 original/web/thumb 키를 재사용하고 이미지 처리를 다시 예약하지 않는다. 최초 공개는 기존 파이프라인을 따른다.
- 인프라 변경 없음. 일괄 조회에 필요한 새 IAM 권한을 추가하지 않고 기존 GetItem 권한을 재사용한다.
- 격리 테스트: `node --experimental-strip-types --experimental-test-module-mocks --test scripts/test-post-management.mjs`.
- Production 회귀: 기존 테스트 글의 제목/관계 수정 반영(200), 원본 관계 유지, Public→Family 상세404/목록제외, 재공개200, 휴지통404, 가족 보관 복구 확인. 사진 테스트 글도 동일 상태 전환을 확인했으며 사진첩에서 제외되고 재공개 시 기존 web 키가 유지됨을 확인했다. 테스트 글 두 건은 최종적으로 FAMILY에 보존했다. 실제 가족 게시물은 수정하지 않았다.
- 최종 Netlify 배포: `6aa0a86f6bd1fec3d692b889`. build 성공, 관련 lint 오류0. 역할 제한 및 동의 부족 차단은 격리된 실제 함수 테스트로 확인했다.
